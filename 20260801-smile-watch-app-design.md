# 笑顔ウォッチ 使用設計書

## 1. 目的

PC のインカメラで作業中の表情を継続的に読み取り、笑顔が減った状態が続いたら画面上で声をかける。カメラ映像は端末内で処理し、外部送信も保存もしない。

## 2. 構成

| 項目 | 内容 |
|---|---|
| 形態 | 単一 HTML ファイル（`smile-watch.html`） |
| 実行環境 | Chrome / Edge / Safari（デスクトップ） |
| 表情解析 | MediaPipe Tasks Vision `FaceLandmarker`（blendshapes） |
| 依存 | CDN から ESM と WASM とモデルを読み込み（初回のみ通信） |
| サーバー | ローカル HTTP サーバーが必須（`file://` ではカメラが使えない） |

## 3. 起動手順

```bash
cd <smile-watch.html のあるディレクトリ>
python3 -m http.server 8000
```

ブラウザで `http://localhost:8000/smile-watch.html` を開き、「見守りを始める」→ カメラ許可。

## 4. 画面

| 領域 | 表示 |
|---|---|
| ヘッダー | アプリ名 / 状態ランプ / 開始・停止ボタン |
| スコア | 直近 1 分の笑顔スコア（0–100） |
| リボン | 直近 5 分の履歴。1 秒 = 1 本の縦帯。低い＝青紫、高い＝山吹色。無人の秒は灰色 |
| 励ましカード | 判定成立時に出現。20 秒で自動的に消える |
| フッター | カメラプレビュー（小） / しきい値・判定時間スライダー / 注意書き |

## 5. 判定ロジック

```
サンプリング       200ms 間隔（5 fps）
生スコア           (mouthSmileLeft + mouthSmileRight) / 2 × 100
表示スコア         直近 60 秒の平均（顔が写っていないサンプルは除外）
ウォームアップ     有効サンプル 30 秒分が貯まるまで判定しない
不在判定           顔を 10 秒以上検出できない間は判定を止める
励まし条件         表示スコア < しきい値 が「判定時間」連続で続く
クールダウン       励まし表示後 5 分間は再判定しない
```

### 設定値

| 設定 | 既定値 | 範囲 |
|---|---|---|
| しきい値 | 20 | 5–60 |
| 判定時間 | 90 秒 | 30 / 60 / 90 / 120 |
| クールダウン | 5 分 | 固定 |

### 励ましメッセージ

8 種類を保持し、直前と同じものを避けてランダム選択する。

## 6. 状態遷移

```
停止中 ──[開始]──> 読み込み中 ──> 見守り中 ─┬─[顔なし10秒]─> 一時停止 ─[顔検出]─> 見守り中
                       │                    └─[条件成立]───> 励まし表示 ──> 見守り中
                       └─[カメラ拒否/失敗]──> エラー表示
        <──[停止]──────────────────────────┘
```

## 7. 制約と注意

- 笑顔スコアは口角の形状のみを見ている。マスク着用・強い逆光・横顔では精度が落ちる。
- 医療・メンタルヘルスの判定ではない。作業中の休憩の目安として使う。
- 初回起動時にモデル（約 3.7MB）を CDN から取得するため、オフラインでは起動できない。継続利用するならモデルと WASM をローカルに置いて参照先を差し替える。
- タブが非アクティブだと `requestAnimationFrame` が止まり、その間は計測されない。

## 8. 動作確認

| # | 手順 | 期待結果 |
|---|---|---|
| 1 | 開始 → カメラ許可 | 状態ランプが「見守り中」、プレビューに自分が映る |
| 2 | 笑顔を作る | スコアが上昇、リボン右端が山吹色に寄る |
| 3 | 無表情を保つ | スコアが 10 以下に下がる |
| 4 | しきい値 60・判定時間 30 秒にして無表情を維持 | 30 秒強で励ましカードが出る |
| 5 | カード表示直後に同条件を維持 | 5 分間は再表示されない |
| 6 | カメラの前から外れる | 状態が「一時停止」、リボンが灰色になる |
| 7 | 停止ボタン | カメラの通電ランプが消え、プレビューが暗転する |
| 8 | カメラ許可を拒否 | エラー文と対処方法が表示され、開始ボタンが戻る |
| 9 | ウィンドウ幅を変える | リボンが幅に追従し、履歴が壊れない |
