# 笑顔ウォッチ 使用設計書

## 1. 目的

PC のインカメラで作業中の表情とまぶたの動きを継続的に読み取り、笑顔が減った状態、または覚醒度が下がった状態が続いたら画面上で声をかける。あわせて計測値を CSV / JSON で書き出せる。カメラ映像は端末内で処理し、外部送信も保存もしない。

## 2. 構成

| 項目 | 内容 |
|---|---|
| 形態 | 単一 HTML ファイル（`smile-watch.html`） |
| 実行環境 | Chrome / Edge / Safari（デスクトップ） |
| 表情解析 | MediaPipe Tasks Vision `FaceLandmarker`（blendshapes） |
| 依存 | 既定は CDN。`npm run fetch-assets` で `vendor/` に置けば通信しない |
| 記録 | IndexedDB。30 日保持 |
| サーバー | ローカル HTTP サーバーが必須（`file://` ではカメラが使えない） |

## 3. 起動手順

```bash
cd <smile-watch.html のあるディレクトリ>
python3 -m http.server 8000
```

ブラウザで `http://localhost:8000/smile-watch.html` を開き、「見守りを始める」→ カメラ許可。

## 4. 画面

| 領域 | 表示 |
|---|---|
| ヘッダー | アプリ名 / 状態ランプ / 開始・停止ボタン |
| 笑顔スコア | 直近 1 分の笑顔スコア（0–100）とリボン |
| 覚醒スコア | 直近 1 分の覚醒スコア（0–100）とリボン、内訳 3 指標 |
| リボン | 直近 5 分の履歴。1 秒 = 1 本の縦帯。無人の秒は灰色<br>笑顔＝低い:青紫 / 高い:山吹　覚醒＝低い:赤茶 / 高い:青緑 |
| 励ましカード | 判定成立時に出現。20 秒で自動的に消える。「笑顔」「覚醒」のどちらで出たかを表示 |
| 自己申告 | いまの調子（眠い / ふつう / 冴えてる） |
| 操作 | カメラプレビュー（小） / しきい値 2 種 / 判定時間 / 通知の可否 / 基準の取り直し |
| 振り返り | 日ごとの平均（直近 14 日）/ 時間帯別の平均 / 要約文 |
| 記録 | 蓄積件数と期間 / 書き出す範囲 / CSV・JSON 書き出し / 記録の消去 |

## 5. 笑顔スコア

```
サンプリング       200ms 間隔（5 fps）
生スコア           (mouthSmileLeft + mouthSmileRight) / 2 × 100
表示スコア         直近 60 秒の平均（顔が写っていないサンプルは除外）
```

## 6. 覚醒スコア

### 6.1 着想

以下を参照した。

- NEC「生理情報認識：顔映像からの眠気（覚醒度）推定技術」
  <https://jpn.nec.com/rd/technologies/201801/index.html>
  まぶたの2つの特徴 ―― **開瞼状態の時間的ゆらぎ**（眠くなると開き具合を一定に保てなくなる）と
  **左右差**（眠くなると左右のまぶたの動きが揃わなくなる）―― を使う。
  どちらも遅い変化なので **5 fps 程度の低フレームレートでも捉えられる**。
- NEDO NEP 採択テーマ「目元の情報から覚醒度を推定する技術の事業化」（阿部 高志）
  <https://nep.nedo.go.jp/selected/bfafd891-f180-48cd-8a47-daabd0587a5a>
  目元の情報のみから覚醒度指標（PVT）を推定する。
- 古典的な **PERCLOS**（一定時間に占める閉眼時間の割合）。眠気指標として最も広く使われている。

本アプリはこれらの考え方を借りた**簡易な近似実装**であり、上記の技術そのものではない。

### 6.2 元になる量

`FaceLandmarker` の blendshape `eyeBlinkLeft` / `eyeBlinkRight`（1 = 閉眼）を反転し、
左右それぞれの**開瞼度** 0–1（1 = 全開）を 200ms ごとに得る。

### 6.3 直近 60 秒から求める 3 指標

| 指標 | 定義 | 対象サンプル |
|---|---|---|
| PERCLOS | 平均開瞼度が **0.20 未満**だったサンプルの割合 | 顔を検出できた全サンプル |
| 開瞼ゆらぎ | 平均開瞼度の**標準偏差** | 平均開瞼度 **0.50 以上**のサンプルのみ |
| 左右差 | 左右の開瞼度の差の絶対値の**平均** | 平均開瞼度 **0.50 以上**のサンプルのみ |

ゆらぎと左右差はまばたきそのものを見たいのではないので、閉じている瞬間を除いた「開瞼中」だけで計算する。
顔を検出できたサンプルが **12 秒ぶんに満たない**間、または 8 個に満たない間はスコアを出さない（`--` 表示）。
個数ではなく時間で見るのは、裏に回るとサンプリングが間引かれるため（7.5 参照）。

### 6.4 個人ベースライン

指標の平常値には個人差・環境差が大きいため、**ウォームアップ 30 秒の直後に一度、その時点の 3 指標を基準として記録する**。
覚醒スコアは基準からの**変化量**で判定する。「いまの状態を基準にする」ボタンでいつでも取り直せる。
基準を取る前は暫定値（PERCLOS 0.05 / ゆらぎ 0.015 / 左右差 0.015）を使う。

### 6.5 スコア化

各指標を「基準からどれだけ離れたか」で 0–1 の減点に直し、重み付けして 100 から引く。

```
減点(指標) = clamp((実測 − 基準) / 幅, 0, 1)

幅        PERCLOS 0.35 / ゆらぎ 0.14 / 左右差 0.12
重み      PERCLOS 0.5  / ゆらぎ 0.3  / 左右差 0.2

覚醒スコア = round( 100 × (1 − Σ 重み × 減点) )        0–100 に丸める
```

開瞼中のサンプルが 6 個に満たない場合（＝ほぼ閉眼）は、ゆらぎ・左右差の減点を満点として扱う。
サンプリングが粗いときは PERCLOS を外し、残る 2 つの重みを配分し直す（7.5 参照）。

想定される出方（シミュレーション値、目安）:

| 状態 | 覚醒スコア |
|---|---|
| 平常 | 100 |
| やや疲れ | 70 台 |
| 眠気あり | 40 前後 |
| 強い眠気 | 10 台 |
| ほぼ閉眼 | 0 |

## 7. 判定ロジック

```
ウォームアップ     開始から 30 秒は判定しない
不在判定           顔を 10 秒以上検出できない間は判定を止める
励まし条件         笑顔スコア < 笑顔しきい値、または
                   覚醒スコア < 覚醒しきい値 が「判定時間」連続で続く
優先順位           両方成立したときは覚醒（眠気）を優先する
クールダウン       励まし表示後 5 分間は種類を問わず再表示しない
```

### 設定値

| 設定 | 既定値 | 範囲 |
|---|---|---|
| 笑顔しきい値 | 20 | 5–60 |
| 覚醒しきい値 | 45 | 20–80 |
| 判定時間 | 90 秒 | 30 / 60 / 90 / 120 |
| クールダウン | 5 分 | 固定 |

### 励ましメッセージ

笑顔 8 種類、覚醒（休憩・仮眠を促す内容）8 種類。種類ごとに直前と同じものを避けてランダム選択する。

## 7.5 裏で動いているときの扱い

計測ループは `requestAnimationFrame` ではなく `setTimeout`。`requestAnimationFrame` はタブが
裏に回ると止まるため、別のアプリで作業している間はまったく計測できなかった。

ブラウザは裏のタブのタイマーを間引くので、サンプリングは粗くなる。それに合わせて次の 3 つを変えた。

| 変更 | 理由 |
|---|---|
| 有効サンプルの判定を個数から時間に変えた（12 秒ぶん + 最低 8 個） | 個数で決めると、間引かれたときに条件が変わってしまう |
| 1 行に「おおう秒数」（`spanSec`）を持たせた | 間引かれると 1 行が数秒〜数十秒をおおう。行数を秒数として数えると実時間とずれる |
| 毎秒 2.5 回を下回ったら PERCLOS を外す | まばたきは 0.1〜0.4 秒。それを取りこぼす頻度では PERCLOS が当てにならない |

PERCLOS を外したときは、残る開瞼ゆらぎ・左右差の重み（0.3 / 0.2）を合計 1 になるよう配分し直す。
どちらも NEC が「遅い変化なので低フレームレートでも捉えられる」としている特徴なので、
粗いサンプリングでも意味が残る。画面には外している旨を表示し、書き出しにも `hz` を残す。

12 秒ぶんのサンプルが集まらないほど間引かれた場合は、覚醒スコアを出さない。

## 7.6 通知

カードはそのタブを見ていないと気づけない。`document.hidden` のときだけ、Notification API で届ける。
許可はチェックボックスの操作をきっかけに求める（ユーザー操作なしでは求められないため）。
`tag` を固定して、たまった通知が積み上がらないようにする。許可が取り消されていることもあるので、
保存した設定だけを信じず、読み込み時に `Notification.permission` を確認し直す。

## 8. データ書き出し

1 秒ごとに 1 行を **IndexedDB**（`smile-watch`）に蓄積する。タブを閉じても再読み込みしても残る。
見守りの開始・停止をまたいで保持し、`session` 番号で区別する。番号自体も `meta` ストアに持つので、
再読み込みしても続きから振られる。

| ストア | 内容 |
|---|---|
| `samples` | 1 秒ごとの行。`epochMs` に索引を張る |
| `events` | 開始 / 停止 / 基準取得 / 励まし表示。同じく `epochMs` に索引 |
| `meta` | セッション番号などの単票 |

### 書き込みのまとめ方

1 秒ごとにトランザクションを張ると重いので、いったん配列に貯め、5 秒ごとに 1 トランザクションで流す。
`pagehide` と `visibilitychange`（隠れたとき）でも流すので、タブを閉じても取りこぼさない。
書き込みに失敗した分は配列に戻し、次の機会に再挑戦する。

### 保存期間

30 日。起動時に、それより古い行とイベントを索引つきカーソルで消す。

### 保存できない環境

プライベートウィンドウなどで IndexedDB を開けないことがある。その場合は保存を諦めてメモリだけで動き、
「タブを閉じると消えます」と画面に出す。このときに限り、未保存の行があれば離脱前に確認ダイアログを出す。

### 記録の消去とセッション境界

見守り中に記録を消すと現在セッションの `start` イベントも消え、以後の行が開始イベントのないまま
書き出されてしまう。そのため消去後に `start`（`afterClear: true`）を入れ直し、
**書き出したファイル中のすべての `session` 番号に対応する `start` イベントが必ず存在する**状態を保つ。

### 記録項目

| 列 | 内容 |
|---|---|
| セッション | 見守り開始ごとに増える通し番号 |
| 記録時刻 | ローカル時刻 `YYYY-MM-DD HH:MM:SS` |
| 経過秒 | そのセッション開始からの経過秒 |
| この行がおおう秒数 | 通常は 1。裏に回って間引かれると大きくなる |
| 顔検出 | その 1 秒に顔を検出できたか（1/0） |
| 笑顔スコア | その 1 秒の平均（0–100） |
| 笑顔スコア_1分平均 | 直近 60 秒の平均（0–100） |
| 覚醒スコア | 直近 60 秒から求めた値（0–100） |
| PERCLOS / 開瞼ゆらぎ / 左右差 | 6.3 の 3 指標 |
| 平均開瞼度 | 直近 60 秒の平均開瞼度（0–1） |
| サンプリング毎秒 | 直近 60 秒の実効サンプリング。2.5 未満なら PERCLOS を外している |

値が求まらない秒は空欄（JSON では `null`）。

### 形式

| 形式 | 内容 |
|---|---|
| CSV | 上表の 1 行 1 秒。Excel 向けに UTF-8 BOM 付き、改行 CRLF |
| JSON | 上記に加えて設定値・個人ベースライン・列の説明・イベント履歴（開始 / 停止 / 基準取得 / 励まし表示）を含む |

書き出す範囲は「すべて / 今日 / 過去 7 日」から選ぶ。書き出しの前に必ず書き込み待ちを流し、
保存済みと合わせて時刻順に並べる。

ファイル名は `smile-watch-YYYYMMDD-HHMMSS.csv` / `.json`。
書き出されるのは数値だけで、映像・画像は一切含まれない。

## 8.4 自己申告

覚醒スコアは実際の眠気と対応づけた検証をしていない。体感を同じ土俵に並べて記録しておけば、
あとから「このスコアは当たっているのか」を数字で確かめられる。

「眠い / ふつう / 冴えてる」を `report` イベントとして残す（`1` / `2` / `3` キーでも）。
見守り中だけ受け付ける。停止中はスコアが無く、突き合わせに使えないため。
ボタンを `disabled` にするだけではキーボード経路が素通りするので、`report()` 自身でも弾く。

イベントには、申告の種別と 0/50/100 の数値に加えて、そのときの覚醒スコア・笑顔スコア・
PERCLOS・ゆらぎ・左右差・サンプリング毎秒・基準を入れる。**重みや幅を詰め直すときの材料**に
なるので、スコアだけでなく生の指標も残す。

振り返りには次を出す。

- 申告ごとの平均覚醒スコアと回数
- 9 回を超えたら、申告値（0/50/100）と覚醒スコアのピアソン相関
- 眠い→ふつう→冴えてる の順にスコアも上がっているか

順序が食い違っていれば、しきい値や重みの見直しが要ると表示する。
どちらか一方が一定だと相関は定義できないので、その場合は出さない。

### 設定の調整（tools/calibrate.mjs）

申告には生の指標と基準が入っているので、スコアの計算を手元で再現できる。
`SPAN` と `WEIGHT` を総当たりで振り、申告値（0/50/100）との相関がいちばん高くなる組を探す。

| 決めごと | 理由 |
|---|---|
| 12 件未満、またはどれかの申告が 3 件未満なら提案しない | 少ない点に合わせ込んだ値は当てにならない |
| サンプリングが毎秒 2.5 回未満の申告は使わない | PERCLOS が当てにならない条件のものを混ぜない |
| 相関の改善が 0.05 未満なら「変えなくてよい」と答える | 誤差の範囲で値をいじらせない |
| しきい値は同点の中央を採る | きれいに分かれると同成績の値が幅を持つ。端はばらつきで崩れる |
| 変えないと答えるときは、しきい値も現行設定で出す | 採らない設定を前提にした助言にしない |

提案するだけで書き換えはしない。指標ごとの単独相関も出すので、効いていない指標が分かる。

## 8.5 振り返り

保存済みの行を集計して、日ごと（直近 14 日）と時間帯別（0–23 時）の平均を出す。
30 日ぶんは数十万行になりうるので、`getAll` で持ち上げず、`epochMs` 索引のカーソルで
足しながら進む。読み込み時と、見守りを止めたとき、記録を消したとき、「更新」を押したときに作り直す。

| 決めごと | 理由 |
|---|---|
| 顔を検出できた行だけを数える | 席を外していた時間を計測時間や平均に混ぜない |
| 時間帯は計測 5 分未満なら平均を出さない | たまたま少し映っただけの値に引っぱられない |
| 平均は計測秒数で重みづけする | 5 分しか計測しなかった日と 3 時間の日を同じ重みにしない |

グラフは 1 区画に覚醒と笑顔を並べた棒。リボンは「色＝スコアの高さ」だが、こちらは 2 系列を
見比べる図なので、高さだけで値を表し、色は凡例と同じ単色にする。

要約文は「直近 14 日で 28 時間 12 分 を計測しました。平均は覚醒 71、笑顔 25 です。
いちばん下がるのは 14 時台（覚醒 58）です。」の形。時間帯の比較は、平均を出せる時間帯が
3 つ以上あるときだけ添える。

## 8.6 設定の保存

しきい値 2 種と判定時間を `meta` ストアに持ち、次に開いたときに復元する。

## 9. 状態遷移

```
停止中 ──[開始]──> 読み込み中 ──> 見守り中 ─┬─[顔なし10秒]─> 一時停止 ─[顔検出]─> 見守り中
                       │                    ├─[+30秒]──────> 基準を記録
                       │                    └─[条件成立]───> 励まし表示 ──> 見守り中
                       └─[カメラ拒否/失敗]──> エラー表示
        <──[停止]──────────────────────────┘
```

## 9.5 解析アセットの取得先

`vendor/manifest.json` を取りに行き、あればそこに書かれたパスから、なければ CDN から読む。
判定はモデル読み込みのときに一度だけ行い、結果を画面下の注意書きに反映する。

| 対象 | 既定 | 同梱時 |
|---|---|---|
| ESM バンドル | jsDelivr | `vendor/vision_bundle.mjs` |
| WASM | jsDelivr | `vendor/wasm` |
| モデル | storage.googleapis.com | `vendor/face_landmarker.task` |
| フォント | Google Fonts | `vendor/fonts.css` |

`npm run fetch-assets` が用意する。MediaPipe 本体は devDependency から複写するので、
テストが使うものと必ず同じ版になる。モデルとフォントだけは取得する。合計 30MB ほどあるため
リポジトリには含めない。

フォントは `<link>` を静的に置かず script 側で挿し込む。同梱がある場合に Google へ
一度も出ないようにするためで、代わりに読み込みがわずかに遅れる。

## 10. 制約と注意

- 笑顔スコアは口角の形状のみを見ている。マスク着用・強い逆光・横顔では精度が落ちる。
- 覚醒スコアは**まぶたの見た目だけ**から求めた簡易指標。眼鏡の反射・伏し目がちな姿勢・照明の変化・カメラの画角でも動く。
  眠気そのものを測っているわけではなく、PVT や脳波と対応づけた検証も行っていない。
- 減点の「幅」と「重み」は経験的に置いた値で、臨床的な裏づけはない。
- ベースラインは「その時点が平常」という前提に立つ。最初から眠い状態で基準を取ると眠気を検出しにくくなる。
  その場合は目が覚めているときに「いまの状態を基準にする」で取り直す。
- 医療・メンタルヘルスの判定ではない。作業中の休憩の目安として使う。
- 既定ではモデル（約 3.7MB）を CDN から取得するため、オフラインでは起動できない。`npm run fetch-assets` で解消する。
- タブが非アクティブだと `requestAnimationFrame` が止まり、その間は計測されない。
- 記録はブラウザごと・オリジンごとに別。ブラウザのデータを消すと記録も消える。
- 保存量は 1 日 8 時間の利用で約 29,000 行、30 日で約 86 万行。書き出しは範囲を絞れるようにしてあるが、
  「すべて」を選ぶとかなり大きなファイルになる。

## 11. 自動テスト

`npm test`（`tests/`）で、実カメラなしの回帰テストを実行する。MediaPipe をスタブに差し替え、
「どんな顔が写っているか」をテスト側から与える。詳細は [tests/README.md](tests/README.md)。

時間の定数は 12 倍速にするが、**1 窓あたりのサンプル数は実機と同じ 300 に保つ**。
計測ループは `requestAnimationFrame`（約 16.7ms）で回るため、サンプリング間隔だけを詰めても
それ以上は速くならず、窓の中身が薄くなって覚醒スコアが出なくなる。

覚醒指標 / 励ましの優先順位とクールダウン / 不在と復帰 / 再キャリブレーション /
保存（まとめ書き・再読み込み後の残存・保存期間切れの破棄）/ CSV・JSON の書き出しと範囲指定 /
記録の消去とセッション境界 / 停止と再開 / 文面 / 同梱アセットの選択 / 振り返りの集計 /
設定の保存 / 裏で動いているときの扱いと通知 / 自己申告と相関 / 実行時エラー の 88 項目を確認する。
GitHub Actions で push と Pull Request のたびに実行する。

このテストが言えるのは実装が仕様どおり動くことまでで、**覚醒スコアが実際の眠気と
どれだけ対応するかについては何も言えない**（スタブが blendshape の値を直接与えているため）。

## 12. 手動での動作確認

| # | 手順 | 期待結果 |
|---|---|---|
| 1 | 開始 → カメラ許可 | 状態ランプが「見守り中」、プレビューに自分が映る |
| 2 | 笑顔を作る | 笑顔スコアが上昇、左リボン右端が山吹色に寄る |
| 3 | 無表情を保つ | 笑顔スコアが 10 以下に下がる |
| 4 | 12 秒ほど顔を写す | 覚醒スコアと PERCLOS / ゆらぎ / 左右差 に数値が出る |
| 5 | 30 秒経過 | 3 指標の下に「基準」の値が入る |
| 6 | 目を半分閉じた状態を保つ | 覚醒スコアが下がり、右リボン右端が赤茶に寄る |
| 7 | 覚醒しきい値 80・判定時間 30 秒にして 6 を維持 | 30 秒強で「覚醒」タグの励ましカードが出る |
| 8 | 笑顔しきい値 60・判定時間 30 秒にして無表情を維持 | 30 秒強で「笑顔」タグの励ましカードが出る |
| 9 | カード表示直後に同条件を維持 | 5 分間は再表示されない |
| 10 | カメラの前から外れる | 状態が「一時停止」、リボンが灰色になる |
| 11 | 「いまの状態を基準にする」 | 基準の値が更新され、覚醒スコアが 100 付近に戻る。直後に励ましは出ない |
| 12 | CSV で書き出す | BOM 付き UTF-8 の CSV が保存され、Excel で文字化けしない |
| 13 | JSON で書き出す | 設定・基準・イベント・全サンプルを含む JSON が保存される |
| 14 | 停止 → 再開 → 書き出す | `session` が 1 と 2 に分かれて記録されている |
| 15 | 記録を消す | 確認ダイアログの後、件数が 0 になり書き出しボタンが無効になる |
| 15b | 見守り中に記録を消し、しばらく置いて書き出す | `start`（`afterClear`）が入っており、行の `session` に対応づく |
| 15c | しばらく計測してタブを閉じ、開き直す | 件数が引き継がれ、セッション番号も続きから振られる |
| 15d | `npm run fetch-assets` 後に機内モードで開く | 起動でき、注意書きが「外部との通信はありません」になる |
| 15e | 通知を有効にし、別のアプリに切り替えてしばらく置く | 励ましが OS の通知で届く |
| 15f | 15e のあと画面に戻り、CSV を書き出す | 裏にいた間の行の `おおう秒数` が 1 より大きく、`サンプリング毎秒` が下がっている |
| 16 | 停止ボタン | カメラの通電ランプが消え、プレビューが暗転する |
| 17 | カメラ許可を拒否 | エラー文と対処方法が表示され、開始ボタンが戻る |
| 18 | ウィンドウ幅を変える | リボン 2 本が幅に追従し、履歴が壊れない |
